import { Action, EventSelector, Extension } from "./model/extension";
export interface ExtensionInfo {
    namespaces?: string[];
    resources?: string[];
    action: Action;
    sync: boolean;
    responds: boolean;
    finalizer: boolean;
    order: number;
    sealResource?: boolean;
}
export declare function toExtension(extensionInfo: ExtensionInfo): Extension;
export declare function prepareEventSelector(extensionInfo: ExtensionInfo): EventSelector;
export declare function extensionInfoToString(extensionInfo?: ExtensionInfo): string;
