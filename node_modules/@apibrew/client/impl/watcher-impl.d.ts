import { Watcher } from "../watcher";
import { Entity } from "../entity";
import { EntityInfo } from "../entity-info";
import { Client } from "../client";
import { Event } from "../model/extension";
export declare class WatcherImpl<T extends Entity> implements Watcher<T> {
    private client;
    private entityInfo;
    private consumer;
    stillRunning: boolean;
    isStopped: boolean;
    constructor(client: Client, entityInfo: EntityInfo, consumer: (event: Event) => void);
    start(): void;
    private run;
    private isRunning;
    stop(): void;
}
