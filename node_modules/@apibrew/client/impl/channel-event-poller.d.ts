import { Client } from '../client';
import { Event } from "../model/extension";
export declare class ChannelEventPoller {
    private client;
    private channelKey;
    private threadName;
    private consumer;
    constructor(client: Client, channelKey: string, threadName: string, consumer: (event: Event) => void);
    private stillRunning;
    private isStopped;
    start(): void;
    private ensureCanRun;
    private run;
    private isRunning;
    close(): void;
}
