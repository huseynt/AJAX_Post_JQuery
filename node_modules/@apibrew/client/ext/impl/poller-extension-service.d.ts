import { ExtensionService } from "../extension-service";
import { Event } from "../../model";
import { Client } from "../../client";
import { AbstractExtensionService } from "./abstract-extension-service";
import { ExternalCall } from "../../model/extension";
export declare class PollerExtensionService extends AbstractExtensionService implements ExtensionService {
    private channelKey;
    private poller;
    constructor(serviceName: string, client: Client, channelKey: string);
    handleEvent(event: Event): void;
    run(): Promise<void>;
    close(): void;
    protected prepareExternalCall(): ExternalCall;
}
