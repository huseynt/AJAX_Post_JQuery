export function label(record) {
    var nameProps = ['name', 'username', 'title', 'label', 'id'];
    if (!record) {
        return '';
    }
    for (var _i = 0, nameProps_1 = nameProps; _i < nameProps_1.length; _i++) {
        var nameProp = nameProps_1[_i];
        if (record[nameProp]) {
            return record[nameProp];
        }
    }
    if (typeof record === 'object') {
        return '- object -';
    }
    return record + '';
}
