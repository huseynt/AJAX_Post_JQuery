var EventHelper = /** @class */ (function () {
    function EventHelper() {
    }
    EventHelper.shortInfo = function (event) {
        return "[".concat(event.action, "]").concat(event.resource.namespace.name, "/").concat(event.resource.name, "/").concat(event.id);
    };
    return EventHelper;
}());
export { EventHelper };
